#pragma once
#include <string>
#include "model/Model.hpp"

class ModelManager {
public:

    Model* getModel(std::string name) {
        if (name == "human") {
            // return this->humanModel;
        }
    }
};